# Keep `/api` for same-origin deployments and local Vite proxying.
VITE_API_BASE_URL=/api

# 本地开发 API 代理目标；可选，默认值为 http://127.0.0.1:3000；仅填写可信的本地后端地址，禁止在地址中携带凭证。
MODELMESH_API_PROXY_TARGET=http://127.0.0.1:3000
